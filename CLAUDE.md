@AGENTS.md

# Maintain Emil's action items (`TODO_EMIL.md`)

Always keep `TODO_EMIL.md` at the repo root current. It is Emil's running checklist of steps **only he can do** — creating accounts, verifying domains, adding DNS records, supplying API keys, flipping dashboard switches.

- Whenever work produces a task that requires Emil's hands-on action (anything Claude cannot do itself), **add it to `TODO_EMIL.md`** with: what to do, exact step-by-step instructions, and a plain-language *why it matters*.
- When such a task is completed, **move it to the Done section** with the date.
- Update the "Last updated" date on every change.
- Emil is newer to devops/infra — write the instructions and the "why" in plain, non-jargon language.
